# Express CORS App

This is a simple Express.js application with CORS enabled.

## Installation

1. Clone the repository.
2. Install dependencies:
   ```bash
   npm install
   ```

## Running the application

```bash
npm start
```

The application will start on `http://localhost:3000`.
